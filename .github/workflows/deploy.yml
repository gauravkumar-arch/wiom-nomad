name: Deploy to Railway

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Railway Deploy Hook
        run: |
          curl -fsSX POST "${{ secrets.RAILWAY_DEPLOY_HOOK_URL }}"
